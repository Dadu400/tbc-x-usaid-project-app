const api_link = "https://dummyjson.com"

const requests = {
    fetchGroceries: `${api_link}/products/category/groceries`,
    fetchRecipes: `${api_link}/recipes/tag/Vegetarian`,
}

export default requests;